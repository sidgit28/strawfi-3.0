{
  "name": "fintech-multiverse-knowledge-repo",
  "version": "1.0.0",
  "description": "Backend API for Fintech Multiverse + Knowledge Repository",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "kill-port": "node scripts/kill-port.js",
    "install-python-deps": "pip install -r python_services/requirements.txt",
    "setup": "npm install && npm run install-python-deps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "axios": "^1.6.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1",
    "openai": "^4.20.0",
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
